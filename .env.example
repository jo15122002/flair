# URL du serveur LLM (llama.cpp)
LLM_ENDPOINT=http://localhost:8080/predict

# API pour poster les commentaires (exemple pour GitLab)
GITLAB_API_URL=https://gitlab.example.com/api/v4
GITLAB_PRIVATE_TOKEN=VOTRE_TOKEN_GITLAB

# Paramètre pour gérer la taille des diff (nombre de caractères par chunk par exemple)
DIFF_CHUNK_SIZE=10000
